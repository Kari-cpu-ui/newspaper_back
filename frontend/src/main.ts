import { bootstrapApplication } from '@angular/platform-browser';
import { AppRootComponent } from './app/app-root/app-root'; 

bootstrapApplication(AppRootComponent)
  .catch(err => console.error(err));